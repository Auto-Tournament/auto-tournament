/**
 * The CS2 module's client entry, for the module build (`scripts/modules/cs2/build.ts`): the
 * integration as the default export a code module's `client/index.js` must have.
 */
export { cs2ClientIntegration as default } from './index';
