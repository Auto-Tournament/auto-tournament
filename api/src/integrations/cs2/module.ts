/**
 * The CS2 module's server entry, for the module build (`scripts/modules/cs2/build.ts`): the
 * integration as the default export a code module's `server/index.mjs` must have.
 */
export { cs2Integration as default } from './index';
